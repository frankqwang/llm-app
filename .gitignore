# IDE
.idea/
*.iml
.vscode/
.claude/

# OS
.DS_Store
Thumbs.db

# Local model files (don't commit large .litertlm/.task/.pt — auto-downloaded on first run)
*.litertlm
*.task
*.pt
models/

# VlogPilot / pc-pilot local run outputs (decisions, timelines, rendered mp4s, logs)
.run*

# Third-party repos cloned at root (not part of this project, see pc-pilot/README.md)
ACE-Step-1.5/

# Per-iteration UI dumps + screenshots from device automation
.tmp_*.xml
.ui*.xml
.screen.png
.sample_*.json
